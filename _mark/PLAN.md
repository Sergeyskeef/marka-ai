# MARK: Операционный план (живой документ)

**Последнее обновление:** 2025-09-29

## Принципы работы
- Сначала аналитика существующей реализации, затем — план, затем — реализация.
- Никаких заглушек: каждая функция доводится до рабочего состояния с тестами.
- Отмечаем прогресс в этом файле и в growth журнале (`scripts/growth_hook.py`).
- Безопасные дефолты, простые фич-флаги через ENV.
- ⚙️ Если в проекте уже есть реализация — сначала анализ и доработка/починка, а не параллельная вторая версия.

## Текущий фокус: Память
### Цели
1) Надёжный write-path (в Graphiti) с метаданными.
2) Read-path с гибридным поиском и re-rank по эмбеддингам.
3) Прозрачность: xtrace в ответах API.

### Сделано сегодня
- Включён **MemoryService facade** (флаг `MARK_MEMORY_FACADE=1` по умолчанию).
- Добавлен shim `core/memory/facade_shim.py` (перехват `add_episode` и `hybrid_search`).
- Убран хард-зависимость от OpenAI в контейнере: добавлен локальный stub `openai/`.
- Исправлен httpx http2 (переключатель `HTTPX_HTTP2`, по умолчанию выключен).
- Проведён e2e smoke внутри контейнера: запись + поиск работают.
- ✨ Добавлен **xtrace**: `core/memory/xtrace.py`, интеграция в `memory_service.retrieve()` и `MemoryManager.hybrid_search()`; API эндпоинт `/api/memory/xtrace/last`; в ответ `/api/chat` метаданные теперь включают `xtrace`.
- 🔒 Нормализация метаданных и маскирование секретов: `core/memory/metadata.py`, применяется в `MemoryService.pre_store()`.

### Чек-лист качества
- [x] e2e запись/поиск через MemoryManager
- [x] ENV-фича-флаг и безопасный фоллбек
- [x] Логи без ошибок, health Graphiti ок
- [x] xtrace в API-ответах на /api/chat и /api/memory (встроено; требуется CI-прогон)
- [ ] Тесты pytest зелёные (smoke добавлен, прогон в CI настроить)
- [ ] Документация по конфигам памяти в README

### Следующие шаги
1) **xtrace hardening**: добавить размеры/тайминги, версию схемы, и детальные причины отбора.
2) **Схема метаданных**: user_id/session_id/project_id/source/tags + TTL/retention политика и утилита очистки.
3) **Бенчмарки**: `tests/test_memory_performance.py` на латентность write/read + регресс-бюджеты.
4) **Миграции Graphiti**: версионирование, backfill эмбеддингов для старых узлов.
