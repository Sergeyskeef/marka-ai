# Проверки выпусков

## 2.5.0 — критерии результата, актуальность памяти и защищённые подключения

На 2026-09-19 код `2431e7001bc4a8d4773f10fb5e4aece176961a46` установлен
и независимо проверен после контрольного периода. Предыдущая версия 2.4
сохранена для восстановления. Публикация этого отчёта не меняет байты runtime.

- Заморожены 119 публичных файлов; хеши всех 35 файлов установленного пакета
  совпали с manifest в боте, bridge и runner. Использованы зависимости проверенного
  2.4, новых runtime-зависимостей нет.
- Linux: 591 тест, OK, один пропуск. На Windows полный промежуточный прогон
  содержал 570 тестов; последующие изменения проверены целевыми наборами.
  Окончательный код прошёл [GitHub Actions](https://github.com/Sergeyskeef/marka-ai/actions/runs/35447096706):
  Python 3.11/3.12/3.14 и отдельный контракт Codex.
- 16 проверок runtime, семь синтетических проверок критериев результата и 11
  реальных проверок runner прошли. SICA выполнил по 592 проверки исходной версии
  и кандидата; исправлен предоставленный случай без установки эксперимента.
- Нативный Codex 0.144.1 проверен с 0/1/2 изображениями: исходящий запрос содержит
  `reasoning.effort=high`, сохраняет JSON schema и не содержит нативных инструментов.
  Этот тест использует локальный подставной endpoint без настоящей авторизации.
- Настоящий Engine через рабочие bridge и runner выполнил два заранее
  зафиксированных задания: структурированный отчёт и исправление программы
  при неизменном проверочном файле. Оба результата прошли исходные критерии:
  18 вызовов модели, 154,24 с, по два продолжения очереди на задание. Файлы
  временные, реальные пользовательские данные не использовались; отправок
  в Telegram не было. Хеш набора:
  `6399098d8b1104f7887f97d351861fe8c30234f567b192b0ec883b5f55314e3e`.
- Новые RPC проверены через настоящий Unix-сокет с UID 10001/10002: отсутствие
  прямого чтения секретов, фиксированные подключения, кеш и объединение проверок,
  пагинация снимков, отклонение произвольных путей/URL/методов, независимый аудит
  после перезапуска bridge. В этом испытании ключи и сервисы синтетические,
  сеть отключена.
- Рабочая установка затем прочитала все пять разделов диагностики, включая
  многостраничные исходники с проверкой хеша. Проверки подключений подтвердили
  локальный вход Codex, Telegram bot identity и OpenAI model metadata. Последнее
  не подтверждает оплату или реальную расшифровку аудио.
- Observer прошёл отдельную проверку под настоящими ограничениями systemd.
  Процедура выпуска прошла 24 проверки, включая отказы и откат: Docker/systemd
  имитировались, SQLite была настоящей. Это отдельно от реальной установки.
- После установки независимо проверены точные образы, хеши, mounts/UID/network,
  контрольные суммы исходной истории, SQLite, heartbeat, observer и готовность
  подключений. Перезапусков и аварийного восстановления не было; соседние
  сервисы Hermes сохранили рабочее состояние. OAuth и ключи не копировались.

Явная настройка модели — `gpt-5.6-sol`, рассуждение `high`; вход остаётся через
подписку Codex. Запрошенные настройки и фактически раскрытое провайдером имя
различаются: `*_resolved` остаются неизвестными. Прохождение двух публичных
заданий не доказывает общее улучшение интеллекта или преимущество перед 2.4.
См. [критерии результата](EVALUATION.md), [подключения](CONNECTIONS.md),
[диагностику](SERVER_READ.md), [память](MEMORY.md) и [модель](MODELS.md).

## 2.4.0 — историческая проверка защищённых самообновлений

На 2026-09-19 выпуск `57ad4f1c380f68caae33996d0f8ef5a186de35ce`
установлен и проверен в защищённом режиме. Контракт и команды:
[SELF_UPGRADES.md](SELF_UPGRADES.md).

- Замороженный Git-архив: 103 публичных файла. Хеши всех 31 установленных файлов
  пакета сверены с исходным manifest в обоих образах; зависимости сохранены от
  проверенного 2.3. Номер версии сам по себе не использовался как проверка кода.
- Windows: 492 теста, OK, 16 пропусков для среды. Linux: 492 теста, OK, 1 пропуск.
  [GitHub Actions](https://github.com/Sergeyskeef/marka-ai/actions/runs/35442424590):
  Python 3.11/3.12/3.14 и контракт Codex — четыре успешных job.
- 16 детерминированных проверок результата и 11 реальных проверок runner прошли.
  Нативный Codex 0.144.1 проверен с 0/1/2 изображениями без внешнего API.
- SICA: 493 проверки у исходной версии и кандидата. Кандидат исправил один
  предоставленный синтетический случай без регрессий. Проверка записи документа
  20 MiB пропускается только под ограничением файла runner в 16 MiB; этот размер
  отдельно проверен через настоящий Unix-сокет bridge и его файловую систему.
- Bridge: реальные UID 10002/10001, сокет, маскирование секретов, блокировка второго
  процесса, изображения и документ 20 MiB, отмена при разрыве соединения,
  квитанции без повторных эффектов и воспроизведение входящих после сброса БД.
  Telegram и модель в этом испытании были синтетическими, сеть отключена.
- Guardian: 13 реальных Docker-сценариев, включая сборку и установку кандидата,
  пробный период, отказ устаревшему запросу, падение, зависание, повреждение БД,
  несовместимую схему, падение при ожидании простоя, перезапуск контроллера во
  время активации и `/rescue` при мёртвом боте. Аварии создавались отдельным
  синтетическим процессом. Диск не заполняли: нехватка места проверена подменой
  результата проверки свободного места; аварийный резерв проверен отдельно.
- Под настоящими ограничениями systemd проверены Docker inspect, сборка overlay
  без сети и запуск fixture. Ограничения сервиса не ослаблялись.

В рабочей установке сохранились все 3317 исходных событий и схема базы.
Авторизация Codex осталась в исходном каталоге; изменён владелец inode, включая
служебные ссылки без перехода по ним. Копирования OAuth не было. Telegram ingress,
ключи и квитанции вынесены в отдельный закреплённый bridge; резервная копия и
контроллер недоступны основному боту. Голосовые продолжают использовать OpenAI API.

Независимая проверка после стартового периода guardian подтвердила точные образы,
хеши кода, права, сохранность источников, heartbeat, `doctor.ready=true`, отсутствие
перезапусков и восстановлений. Все три соседних сервиса Hermes остались активны.
Отдельный свежий запрос с уникальным ID получил правильный ответ от Codex через
bridge за 5,72 секунды; сообщений в Telegram при этой проверке не отправляли.
Настройка модели оставлена по умолчанию, точное внутреннее имя не подтверждено.

Эти проверки подтверждают механизм установки и восстановления; они не доказывают
рост качества рассуждений на произвольных задачах. Реальный владелец ещё должен
оценивать полезность конкретных самоизменений. Резервная копия канонической БД
сохранена также на компьютере; произвольные рабочие файлы и полная потеря сервера
не покрываются одним локальным откатом. Свободного места на сервере около 1,9 GB;
регулярные эксперименты требуют расширения диска или дополнительного места.

Ниже сохранены исторические результаты 2.3 и более ранних выпусков. Указанные
там ограничения самообновления относятся к соответствующим прежним версиям.

# Проверки выпуска 2.3.0

Дата: 19 сентября 2026 года, UTC+3. Выпуск добавляет распознавание голосовых
через OpenAI API, календарное время в контексте, устойчивые идентификаторы
действий длинных задач и исправления извлечения и применения опыта.

## Голосовые и проверка модели

- Один настоящий запрос `gpt-4o-transcribe` распознал синтетическую русскую
  OGG/Opus-запись длиной 7,843 с за 1,503 с. **10/10 проверок**: сохранение
  источника, расшифровки, задачи и восстановление без второго API-запроса.
  Локальная модель речи не устанавливается. Используется отдельный файл
  существующего API-ключа; ключ отсутствует в репозитории и контексте модели.
- Расшифровка сохраняется как `unverified_transcription`. Принятые факты
  автоматически не создаются. Это синтетический тест; реальное голосовое
  владельца через Telegram им не проверено. [Подробности](VOICE.md).
- Настоящий Codex прошёл **3/3 сценария, 7 вызовов, 45,47 с**:
  напоминание на завтра в 09:00 UTC+03; выбор пользовательского первоисточника
  при противоречии с ответом ассистента; чтение нужной страницы документа
  длиной 35 000 символов. Рабочий gateway восстановлен после 67,57 с
  с прежними образами. Сценарии не отправляли сообщения через Telegram.

## Надёжность и память

Проверки длинной задачи подтверждают 40 разных действий через несколько
продолжений без слияния напоминаний и отправок файлов. Повтор календарного
действия возвращает прежнюю квитанцию, даже если назначенный срок уже прошёл.
Команда `/cancel` без номера не отменяет задачи. Голосовое с исчерпанным
бюджетом блокируется до платного запроса и требует `/extend`.

Отрицательная оценка подтверждённого повторного применения навыка исключает
его из автоматического использования. Повреждение отдельного смыслового
индекса сохраняет поиск в канонической базе и явно отражается в status.
Качество извлечения проверено на архиве из 3275 сообщений и отдельном наборе
из 12 запросов; результаты и случаи ухудшения приведены в [SEMANTIC.md](SEMANTIC.md).
Исходные тексты и авторизация не публикуются.

---

## История проверок выпуска 2.2.0

Дата: 19 сентября 2026 года, UTC+3. Установленный код —
[`6b84a57`](https://github.com/Sergeyskeef/marka-ai/commit/6b84a571c92e9f00f36181ece84591d24fd45c84).
Выпуск добавляет изображения, ежедневные снимки базы и исправляет выбор Python
в runner. Проверки ниже отделяют модель, транспорт, состояние и работающий сервис.

## Код и изображения

- Linux / Python 3.11: **342 теста, без ошибок, один пропуск** теста,
  которому требуется собственный disposable PID namespace.
- Детерминированные сценарии: **16 из 16**. Отдельный runner: **11 из 11**,
  включая реальный Python с Pillow и запрет рекурсивного вызова runner.
- Реальный Codex CLI `0.144.1` проверен с **нулём, одним и двумя PNG**:
  локальный HTTP-приёмник получил точные байты изображений и JSON schema;
  нативные инструменты отсутствуют, заголовок авторизации не отправляется.
- Настоящая модель прошла путь `Application.ingest → MediaStore → Engine → Codex`
  с синтетической картинкой. После повторного update и пересоздания приложения
  сохранились один файл и одна задача. Модель верно прочитала надпись, цвета
  и взаимное расположение фигур, записала JSON и выполнила проверочный assert:
  **3 вызова модели, 17,37 с**, итоговая проверка `observed`.
- В проверке модели использован существующий отдельный вход Марка без копирования
  auth-файла; рабочий сервис был восстановлен после 38,7 с. Telegram download
  заменён синтетическим транспортом: это не проверка нового фото от владельца
  через настоящий Telegram. Принятые воспоминания по картинке не создавались.
- SICA-подобный эксперимент: **343 теста исходной и изменённой версии**;
  кандидат исправил предоставленный случай, установленный код не изменился.
- [GitHub Actions для установленного commit](https://github.com/Sergeyskeef/marka-ai/actions/runs/35401783791):
  Python 3.11/3.12/3.14 и Codex contract check завершились успешно.

## Установка и сохранность

Проверенные образы установлены в существующие постоянные тома. Обновление заняло
38,01 с; сохранены все **3 299 исходных событий** с прежним содержимым и метаданными.
Перед обновлением создан согласованный SQLite backup, образы 2.1 оставлены для отката.
После обновления `doctor` подтвердил исправность базы, привязку владельца,
авторизацию Codex и отсутствие Telegram webhook. Новый runner выполнил Python-вызов;
оба контейнера работают без OOM и перезапусков.

Первая автоматическая копия базы создана сразу: **63 700 992 байта**.
Ежедневные снимки сохраняются локально, максимум семь завершённых дат.
Тесты проверяют восстановление источников, памяти, заданий и доставок,
размер базы с учётом WAL, недостаток места, таймаут, перезапуск и отказ от symlink.
Фотографии, рабочие файлы и авторизация в SQLite-снимок не входят;
он не заменяет отдельный полный backup приватного state-тома.

Полный семантический индекс импортированного архива построен:
**3 307 источников / 17 986 фрагментов**, без внешнего embedding API.
Индексация новых источников продолжается при простое. Наличие индекса не означает,
что каждый запрос найдёт нужное свидетельство; качество извлечения проверяется
отдельно и имеет ограничения, описанные в [SEMANTIC.md](SEMANTIC.md).

---

## История проверок выпуска 2.1.0

Дата: 19 сентября 2026 года, UTC+3. Проверенный код —
[`a4dacf4`](https://github.com/Sergeyskeef/marka-ai/commit/a4dacf4dc3987ff584f51c23a7e2af7bbb563577).
Проверки исходников, настоящей модели, переноса состояния и работающей установки
перечислены отдельно. Личные данные и журналы остаются вне публичного репозитория.

## Код и контейнеры

- Linux / Python 3.11: **304 теста, без ошибок, один пропуск** теста,
  которому требуется собственный disposable PID namespace.
- Детерминированные сценарии оценки: **16 из 16**. Они проверяют наблюдаемые
  результаты, сохранность источников и реакцию на противоречия;
  это не независимый бенчмарк общей интеллектуальной способности.
- Реальный сетевой контракт Codex CLI `0.144.1` с локальным HTTP-приёмником:
  запрос содержит структурированную схему, нативные инструменты отключены.
- Отдельный runner: **10 из 10** проверок Python/Node/Bash, файлов, отсутствия
  сети и credentials, лимитов, таймаутов и завершения дочерних процессов.
- SICA-подобный эксперимент: **305 тестов исходной и изменённой версии**,
  включая один предоставленный регрессионный случай. Кандидат исправил этот
  случай, сохранил остальные проверки; установленный runtime не изменялся.
- [GitHub Actions для проверенного commit](https://github.com/Sergeyskeef/marka-ai/actions/runs/35397840046):
  Python 3.11/3.12/3.14 и отдельная проверка контракта Codex — успешно.

## Настоящий Codex и опыт выполнения

Официальная авторизация Марка использована в отдельном проверочном контейнере;
на время проверки рабочий gateway был остановлен, затем восстановлен.
Файл авторизации не копировался. Telegram в этих сценариях не участвовал.

Два сценария потребовали **12 реальных вызовов модели**:

1. Найти актуальный срок после замены прежней записи памяти — один вызов,
   правильное новое значение. Ответ на вопрос остаётся модельным ответом,
   а не результатом инструментального верификатора.
2. Запустить скрипт с ошибкой, исправить причину, сохранить исходные данные
   и проверочный assert, получить верный JSON, сохранить навык и выполнить
   его повторно — 11 вызовов, два автоматических продолжения задачи.
   Журнал содержит исходный ненулевой код возврата, исправление, успешную
   проверку и успешный `skill.run`. Попытка заменить исполняемый файл навыка
   через параметр данных была отклонена; модель прочитала контракт и исправила
   вызов. Итоговый статус проверки — `observed`.

Локальная модель семантического поиска оставалась загруженной во время Codex.
Контейнер с лимитом 1 GiB завершился с кодом 0, `OOMKilled=false`;
наблюдалось около 503 MiB, точное максимальное потребление cgroup не получено.
Эти два сценария не доказывают надёжность любого будущего поручения.

## Память и развёртывание

Сначала перенос был повторён в отдельном томе: старая версия создала восемь
принятых воспоминаний, десять событий, завершённое задание и ожидающую доставку.
Новая версия перенесла реальный приватный архив. Все прежние строки событий,
знаний, связей с источниками, заданий и доставок совпали с исходными;
владелец и тестовый токен сохранились. Авторизация в эту проверку не подключалась.

Затем те же проверенные образы установлены в постоянный Compose project:

- Сохранены все **23 прежних события**, восемь принятых записей памяти
  и два кандидата; роли и статусы не повышались при импорте.
- **3 246 исторических сообщений из 65 разговоров** импортированы как
  **3 275 частей** с датами, авторами и происхождением. Повторный импорт
  добавил ноль записей и пропустил те же 3 275 частей.
- `retention_days` явно изменён с прежнего значения на `0`: автоматическое
  удаление сырой истории выключено. Политика процедурного обучения `auto`
  включена по поручению владельца, само поручение сохранено с происхождением.
- Созданы согласованные SQLite-копии до переноса и после импорта;
  прежние образы сохранены для отката. Рабочие auth-файлы не заменялись.
- После обновления: база исправна, владелец привязан, Codex authenticated,
  Telegram доступен без webhook, новый runner выполнил настоящий Python-вызов.
  Bot и runner работают без перезапусков и OOM.

Локальная модель поиска проверена по хешам закреплённых исходных файлов
и manifest производного ORT-файла. Лексическая проекция построена при переносе;
полный векторный индекс большого архива строится отдельно в ограниченном
контейнере и не требует вызовов Codex или внешнего embedding API.
Ресурсные измерения и ограничения модели — в [SEMANTIC.md](SEMANTIC.md).

Первый реальный диалог Telegram был подтверждён при запуске 2.0 ниже.
Проверка `doctor` после обновления не выдаётся за новый пользовательский диалог.
Обучение весов, универсальная проверка правдивости ответа и автоматическая
установка собственного изменённого кода не реализованы.

---

## История проверок выпуска 2.0.0

Дата: 18 сентября 2026 года. Проверки кода, настоящего провайдера,
контейнерной среды и Telegram перечисляются отдельно.

## Автоматические проверки

На Windows / Python 3.14: `python -m unittest discover -s tests -q` —
130 тестов, без ошибок; 3 пропуска из-за платформенных условий
(symlink privileges и запуск runner строго внутри контейнера).

На Linux / Python 3.11 в собранном bot container: 130 тестов, без ошибок;
1 пропуск теста, которому нужен собственный disposable PID namespace.
После адаптации большого Telegram test fixture к лимиту sandbox отдельно
повторены 14 тестов транспорта; production-ограничение размера не менялось.

Проверяются реальные SQLite-транзакции и восстановление очереди, дубликаты
Telegram updates, привязка владельца, неопределённая доставка, отмена задач,
бюджеты, происхождение и отзыв знаний, редактирование файлов с историей,
границы путей, subprocess-провайдер и сравнение кандидатов собственного кода.
В тестах приложения модель и Telegram заменены контролируемыми ответами.

## Настоящие интеграции

- Официальный Codex CLI `0.144.1`: mock HTTP endpoint получил настоящий
  запрос Responses с отсутствующим или пустым `tools`. Проверка воспроизводится
  через `scripts/probe_codex_tools.py`; авторизация для неё не нужна.
- Настоящий Codex account login: отдельный вход Марка на сервере завершён;
  минимальный структурированный запрос модели выполнен успешно.
- Полный Engine с настоящим Codex: создание файла, чтение и подтверждение
  содержимого — 3 вызова модели, 2 наблюдения инструмента.
- Публичные HTTPS-чтение и поиск через Bing RSS проверены на реальном запросе.
- Bot и sandbox images собраны и проверены на сервере. Реальный runner прошёл
  10 проверок `scripts/container_smoke.py`: Python/Node/Bash, двоичные файлы,
  отсутствие сети и секретов, запрет рекурсивного вызова, остановка
  отсоединённых процессов, ограничения времени, вывода и размера артефактов.
- Telegram `getMe` и `getWebhookInfo` успешно выполнены для предоставленного
  владельцем бота; активного webhook нет. Токен хранится вне Git.
- Реальный владелец привязался через `/start`; сервер подтвердил `owner_paired`.
  После запуска сохранено завершённое задание из Telegram и три успешные
  доставки, включая служебные ответы. База прошла `quick_check`; восемь
  исходных записей личного контекста присутствуют, создан SQLite backup.
- Постоянные bot и sandbox работают в отдельном Compose project. Новый
  runner socket проверен настоящим Python-вызовом после развёртывания.
  Существующие Hermes gateways остались активны. Проверочные контейнеры,
  тома и временный каталог удалены; постоянное состояние сохранено.
- [GitHub Actions для кода выпуска](https://github.com/Sergeyskeef/marka-ai/actions/runs/35390534180)
  завершился успешно: Python 3.11/3.12/3.14 и отдельный Codex contract check.

## Самоизменение

`/evolve` маршрутизируется через обычный Engine. Изменения проверяются на копии;
два запуска получают одинаковые канонические тесты и, если задан,
одинаковый предлагаемый регрессионный случай. Артефакты — патч, отчёт,
исходная и изменённая версии с хешами. Автоматической установки нет.

`scripts/evolution_smoke.py` выполнил настоящий baseline/candidate эксперимент
в runner: одинаковые 131 тест, добавленный случай маскирования явно вымышленного
Slack-токена провалился на исходной версии и прошёл у кандидата. Остальные
регрессии сохранились. Итог — `improved_on_provided_case`; исходный установленный
модуль не изменился, установка кандидата не выполнялась.

Затем настоящий Engine с Codex account auth самостоятельно повторил сценарий:
4 вызова модели, два `self.inspect` и один `self.experiment`, 131 тест на каждую
версию, исправленный предоставленный случай и итог `completed`.
Это проверка всего пути от поручения до кандидата, исполнения и отчёта;
Telegram в этом эксперименте не использовался.

Результат `improved_on_provided_case` означает исправление конкретного
предоставленного случая. Он не доказывает рост общего качества агента.
Ограничения независимости оценщика описаны в [SICA.md](SICA.md).

## Границы результата

Успешная авторизация и `getMe` сами по себе не заменяют реальный диалог владельца
с ботом. Для проверенной установки привязка и первый диалог выполнены;
при новой установке владелец должен привязаться одноразовым `/start` из личного чата.
Не заявляются обучение весов, независимая оценка качества каждого ответа,
автоматическая установка своего кода или готовность внешних интеграций,
которых в этой версии нет.

История старого проекта сохранена отдельно. Приватные воспоминания,
токен Telegram и Codex auth не включены в репозиторий или эти результаты.
